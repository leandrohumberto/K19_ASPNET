@model CamadaDeApresentacao.Models.Aluno
@{
    ViewBag.Title = "Editar";
}

@section Toolbar 
{
    <input type="button" value="Remover este cadastro" />
}

<h2>Editar</h2>

<form action="/Aluno/Editar" method="post">
    @Html.EditorForModel()

    @*@Html.EditorFor(a => a.AlunoID)

    @Html.LabelFor(a => a.Nome)
    @Html.EditorFor(a => a.Nome)

    @Html.LabelFor(a => a.Email)
    @Html.EditorFor(a => a.Email)*@

    @*@Html.HiddenFor(a => a.AlunoID)

    @Html.LabelFor(a => a.Nome)
    @Html.TextBoxFor(a => a.Nome)

    @Html.LabelFor(a => a.Email)
    @Html.TextBoxFor(a => a.Email)*@

    @*@Html.Hidden("AlunoID")

    @Html.Label("Nome")
    @Html.TextBox("Nome")

    @Html.Label("Email")
    @Html.TextBox("Email")*@
    
    @*<label for="Nome">Nome</label>
    <input type="text" id="Nome" name="Nome" value="@Model.Nome" />

    <label for="Email">E-mail</label>
    <input type="email" id="Email" name="Email" value="@Model.Email" />*@

    <input type="submit" value="Salvar" />
</form>

